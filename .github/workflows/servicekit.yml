name: Service Checks

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    uses: fracturing-space/servicekit/.github/workflows/reusable-go-service.yml@main
    with:
      servicekit-version: main
      use-hooks: true
      run-race: true
